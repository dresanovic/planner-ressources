# FS-020 Accessibility and Usability Evidence

Status: **not executed in this implementation environment**.

Automated DOM tests cover dialog naming/description, safe default focus, Tab and Shift+Tab containment, Escape behavior, busy controls, alert/status semantics, focus restoration, 44-pixel button styling, wrapping rules, and no double-modal action. They do not substitute for the required manual checks.

Before release, record latest stable Edge/Chrome/Firefox on Windows; keyboard-only results; NVDA with Firefox; 200% text zoom; 320 CSS-pixel viewport; long-label behavior; and at least 10 anonymized representative participants or designated acceptance reviewers. Calculate `required_successes = ceil(0.90 * participant_count)` and retain category, outcome, duration, and failed step for the complete find/notice/download/manual-Outlook-import path, static-snapshot comprehension, and repeat-import/manual-removal understanding.
